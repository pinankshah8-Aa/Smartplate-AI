import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ['10.57.190.146', 'localhost', 'curly-renegade-gauze.ngrok-free.dev', 'offices-thin-nokia-cancel.trycloudflare.com']
};

export default nextConfig;
